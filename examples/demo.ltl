// examples/demo.ltl — runnable demo for ltl_union_find

import std::io
use ltl_union_find as lib

fn main() {
    io::println("ltl_union_find demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}