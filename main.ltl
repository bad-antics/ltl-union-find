// ltl-union-find/main.ltl
//
// Union-find / DSU with path-compression + rank.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-union-find v0.1.0")
    io::println("Union-find / DSU with path-compression + rank.")
    0
}
